A = []
B = []
C = []

# Leitura de A
A = input(); 
# Leitura de B
B = input(); 

# Criação de C
contador = 0;
for i in A:
    # se for igual adiciona no conunto
    if(b[contador] == i):
        c.append(i);
    contador+=1;

# tem forma mais facil de fazer isso 
# pesquise sobre conjuntos python no google

print(C) # Não escreva nada além disso